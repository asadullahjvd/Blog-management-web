<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white px-3 py-4">
        <a class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block ml-400" href="/logout">LOGOUT</a>
        <div class="flex items-start gap-3 "><div class="w-10 h-10  rounded-md"><img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>">
 </div>
        <h1 class="text-4xl font-bold text-white mb-5">Greetings, <%= user.name %> — wishing you a productive and joyful day ahead! ✨
</h1></div>
        <h5 class="mb-2">You can create a new post</h5>
        <form action="/post" method="post">
            <textarea class="resize-none bg-transparent border-2 border-white rounded-md w-1/3 p-3"
                placeholder="What's on your mind?" name="content"></textarea>
            <input class="px-3 py-2 bg-blue-500 block  rounded-md mt-2" type="submit" value="Create new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400 mb-2">Your Posts.</h3>
            <div class="postcontainer mt-5 bg-white-500">
                <%user.posts.reverse().forEach(function(post){%>
                    <div class="post w-1/3 p-5 border-2 border-zinc-800 bg-zinc-800 rounded-md mt-4">
                        <h4 class="text-blue-500 mb-2">
                            <%=user.name%>
                        </h4>
                        <p>
                            <%=post.content%>
                        </p>
                        <small class="mt-5 inline-block">
                            <%=post.likes.length%> likes
                        </small>
                        <div class="btns flex gap-10 mt-1">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) ===-1 ? "Like" : "Unike" %>
                            </a>
                            <a class="text-red-500" href="/edit/<%=post._id%>">Edit</a>
                        </div>

                    </div>
                    <%})%>
            </div>

        </div>

    </div>




    <script src="/javascripts/script.js"></script>
</body>

</html>